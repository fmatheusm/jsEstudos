const nomes = ['Eduardo', 'Maria', 'Joana', 'Wallace', 'Rosana'];
// nomes[2] = 'João';
// delete nomes[2];
console.log(nomes);

const novo = nomes.slice(1, 3);
console.log(novo);
const novo2 = nomes.slice(1, -3);
console.log(novo2);
